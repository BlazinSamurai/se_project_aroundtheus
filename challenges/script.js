/* This page is here to work out errors or problems in your javascript
 *  code.
 */

console.log("Hello World!");

/*
addModalForm.addEventListener("submit", (e) => {
  e.preventDefault();

  const cardElement = cardTemplate.cloneNode(true);
  const cardImageEl = cardElement.querySelector(".card__image");
  const cardTitleEl = cardElement.querySelector(".card__title");
  const cardLikeButton = cardElement.querySelector(".card__button-like");

  cardLikeButton.addEventListener("click", () => {
    cardLikeButton.classList.toggle("card__button-like_active");
  });

  cardImageEl.src = addModalURL.value;
  cardTitleEl.textContent = addModalTitle.value;
  cardImageEl.alt = addModalTitle.value;

  cardListEl.prepend(cardElement);
  closeAddModal();
});
*/
